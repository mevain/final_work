<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to "Back to books", books_path %>
  <%if @book.author_email == current_user.email %>
  <%= button_to "Destroy this book", @book, method: :delete %>
  <% end %>
</div>
